// import { GlobalService } from '@/service'

const handleUploadImage = async (blobInfo, progress) => {
  // try {
  //   const formData = new FormData()
  //   formData.append('upload', blobInfo.blob())
  //   const resp = await GlobalService.uploadFile(formData)
  //   return resp.data.url
  // } catch (error) {
  //   throw { error, remove: true }
  // }
}
export { handleUploadImage }
